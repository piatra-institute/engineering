\begin{tikzpicture}[x=1cm, y=0.8cm, font=\small,
    box/.style={draw, rounded corners, minimum width=2.8cm, minimum height=0.8cm,
      align=center, fill=gray!10},
    leaf/.style={draw, rounded corners, minimum width=2.8cm, minimum height=0.8cm,
      align=center, fill=red!15},
    arrow/.style={->}]
  \node[box] (root) at (6, 6) {morphogenetic\\ failure};
  \node[box] (spec) at (1, 4) {specification\\ error};
  \node[box] (trans) at (6, 4) {translation\\ error};
  \node[box] (exec) at (11, 4) {execution\\ error};
  \node[leaf] (no) at (-0.5, 1.5) {no\\ regeneration};
  \node[leaf] (incomp) at (3.5, 1.5) {incomplete\\ regeneration};
  \node[leaf] (wrong) at (8.5, 1.5) {wrong\\ morphology};
  \node[leaf] (uncon) at (12.5, 1.5) {uncontrolled\\ proliferation};
  \draw[arrow] (root) -- (spec);
  \draw[arrow] (root) -- (trans);
  \draw[arrow] (root) -- (exec);
  \draw[arrow] (spec) -- (no);
  \draw[arrow] (spec) -- (incomp);
  \draw[arrow] (trans) -- (incomp);
  \draw[arrow] (trans) -- (wrong);
  \draw[arrow] (exec) -- (wrong);
  \draw[arrow] (exec) -- (uncon);
\end{tikzpicture}
